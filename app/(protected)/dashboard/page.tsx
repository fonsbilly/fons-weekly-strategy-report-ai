export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p style={{ color: "var(--text-muted)" }}>
        Weekly submission status will appear here once the submission form is built.
      </p>
    </div>
  );
}
